{
  "name": "cube-app",
  "version": "1.0.0",
  "description": "Webapp that allows users to build on a plane and share their builds",
  "scripts": {
    "start": "cd server && node bin/www",
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "critical": "node critical.js",
    "dev": "nodemon",
    "lint": "cd client && npm run lint && cd ../server && npm run lint"
  },
  "author": "Amy, Marin, Axel, Bianca",
  "license": "ISC",
  "type": "module"
}
