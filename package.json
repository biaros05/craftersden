{
  "name": "comments-app",
  "version": "1.0.0",
  "description": "Proof of concept which lets users comment",
  "scripts": {
    "start": "cd server && node bin/www",
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "critical": "node critical.js",
    "dev": "cd server && nodemon bin/www",
    "lint": "cd client && eslint . --fix && cd ../server && eslint . --fix"
  },
  "author": "Amy, Marin, Axel, Bianca",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/react": "^19.0.8",
    "@types/react-dom": "^19.0.3",
    "typescript": "^5.7.3"
  }
}
